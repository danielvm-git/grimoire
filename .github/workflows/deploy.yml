name: Deploy
# version: 4.1.0 — central reusable workflow invocation

on:
  workflow_run:
    workflows: ["Test Build Release"]
    types: [completed]

permissions:
  contents: read
  actions: read

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  cd:
    uses: danielvm-git/.github/.github/workflows/deploy-python.yml@main
    with:
      site_url: "https://grimoire.bigbase.click"
    secrets: inherit
